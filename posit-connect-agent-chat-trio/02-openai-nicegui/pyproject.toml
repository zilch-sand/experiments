[project]
name = "openai-nicegui-agent"
version = "0.1.0"
description = "OpenAI Agents SDK + NiceGUI demo for Posit Connect"
requires-python = ">=3.11"
dependencies = [
  "nicegui>=2.10.1",
  "openai-agents>=0.0.12",
]

[tool.uv]
package = false
