[project]
name = "langgraph-gradio-agent"
version = "0.1.0"
description = "LangGraph + Gradio locked-down agent demo"
requires-python = ">=3.11"
dependencies = [
  "gradio>=5.0.0",
  "langgraph>=0.2.60",
  "langchain-openai>=0.2.14",
]

[tool.uv]
package = false
